@model IEnumerable<_CurriculumManagerSystem.Models.Chuandaura_monhoc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.chisocio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.noidung)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.loai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.mahp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.chisocio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.noidung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.loai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.mahp)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.macdmon">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.macdmon">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.macdmon">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
