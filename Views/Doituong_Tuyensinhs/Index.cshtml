@model IEnumerable<_CurriculumManagerSystem.Models.Doituong_Tuyensinh>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.noi_dung_dtts)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.noi_dung_dtts)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ma_dtts">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ma_dtts">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ma_dtts">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
