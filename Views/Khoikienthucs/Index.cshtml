@model IEnumerable<_CurriculumManagerSystem.Models.Khoikienthuc>

@{
    ViewData["Title"] = "Index";
}

@* <h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.kkt_ten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.kkt_mota)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.kkt_ten)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.kkt_mota)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.makkt">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.makkt">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.makkt">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table> *@
<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h3 class="titile-table">KHỐI KIẾN THỨC | <a class="near-titile-table" asp-action="create">Thêm</a>
                </h3>
            </div>
            <div class="body">
                <div class="table-responsive">
                    <table class="table table-bordered table-striped table-hover js-basic-example dataTable">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.kkt_ten)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.kkt_tuchon)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.kkt_mota)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.kkt_ten)
                                    </td>
                                     <td>
                                        @Html.DisplayFor(modelItem => item.kkt_tuchon)
                                    </td>
                                     <td>
                                        @Html.DisplayFor(modelItem => item.kkt_mota)
                                    </td>
                                    <td>
                                        <a class="material-icons" asp-action="Edit"
                                        asp-route-id="@item.makkt">mode_edit</a>
                                        <a class="material-icons" asp-action="Delete"
                                        asp-route-id="@item.makkt">delete_forever</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>