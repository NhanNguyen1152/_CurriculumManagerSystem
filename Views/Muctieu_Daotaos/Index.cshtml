@model IEnumerable<_CurriculumManagerSystem.Models.Muctieu_Daotao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.noi_dung)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.mtdt_chinh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.mtdt_phu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.noi_dung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.mtdt_chinh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.mtdt_phu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ma_mtdt">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ma_mtdt">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ma_mtdt">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
