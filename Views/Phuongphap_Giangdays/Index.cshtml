@model IEnumerable<_CurriculumManagerSystem.Models.Phuongphap_Giangday>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tenpp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tenpp)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.mappday">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.mappday">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.mappday">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
