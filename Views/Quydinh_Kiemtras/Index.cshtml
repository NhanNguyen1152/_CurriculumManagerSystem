@model IEnumerable<_CurriculumManagerSystem.Models.Quydinh_Kiemtra>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tenqd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tenqd)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.maqd">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.maqd">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.maqd">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
