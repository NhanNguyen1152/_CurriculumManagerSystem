@model List<Doituong_Tuyensinh>
<div class="body">
                <form class="form-horizontal" asp-action="CreateListTS">
                    <table class="table  table-sm">
                        <div class="row clearfix">
                            <thead class="table-info">
                                <tr style="background-color: #84c7c1; color: black;">
                                    <th>Lựa chọn</th>
                                    <th>Nội dung</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            <label class="custom-control custom-checkbox">
                                                <input type="checkbox" class="custom-control-input" value="@item.ma_dtts" name="arrayTS" id="@item.ma_dtts">
                                                <span class="custom-control-indicator"></span>
                                            </label>
                                        </td>
                                        <td>@item.noi_dung_dtts</td>
                                    </tr>
                                }
                            </tbody>
                        </div>

                    </table>
                    <div class="row clearfix">
                        <div class="col-lg-offset-2 col-md-offset-2 col-sm-offset-4 col-xs-offset-5">
                            <a asp-action="Index" class="btn btn-default waves-effect" style="margin-left: 82%;">Trở về trang chủ</a> |
                            <button type="submit" class="btn btn-primary waves-effect">Tạo mới</button>
                        </div>
                    </div>
                </form>
            </div>