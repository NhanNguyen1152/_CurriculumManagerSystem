@model _CurriculumManagerSystem.Models.DecuongTailieu
@using Microsoft.AspNetCore.Http;
@using System.Data;
<div class="body">
                <form class="form-horizontal" asp-action="EditDecuongtailieu">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                     <input hidden required asp-for="mahp" value=@Context.Session.GetInt32("id_edit_dccht_after")>
                    <div class="row clearfix">
                        <div class="col-lg-2 col-md-2 col-sm-4 col-xs-5 form-control-label">
                            <label asp-for="matl">Chọn tài liệu tham khảo</label><span class="text-danger">*</span>
                        </div>
                        <div class="col-lg-10 col-md-10 col-sm-8 col-xs-7">
                            <div class="form-group">
                                @* <input asp-for="matl" value=@TempData["id_tl"]> *@
                                <select required class="form-control show-tick" asp-for="matl" asp-items="ViewBag.matl">
                                    <option selected="selected" value="" disabled>Vui lòng chọn một </option>
                                </select>
                            </div>
                        </div>
                    </div>
                   <div class="row clearfix">
            <div class="col-lg-offset-2 col-md-offset-2 col-sm-offset-4 col-xs-offset-5">
                <a asp-action="Index" class="btn btn-default waves-effect" style="margin-left: 83.5%;">Trở về trang chủ</a> |
                <button type="submit" class="btn btn-primary waves-effect">Lưu</button>
            </div>
        </div>
                </form>
            </div>